name: rusterm_cli
description: RusTerm IPC client CLI for testing and Flutter integration
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.2
  ffi: ^2.1.0

dev_dependencies:
  lints: ^2.1.0
